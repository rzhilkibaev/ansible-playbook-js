# {{ ansible_managed }}

[Unit]
Description=docker registry
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a docker-registry
ExecStop=/usr/bin/docker stop docker-registry

[Install]
WantedBy=local.target
